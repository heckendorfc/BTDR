#######################################################

YAML_HOME = @YAML_HOME@

PKG_CPPFLAGS = -I$(YAML_HOME)/include
PKG_LIBS = @BTDR_LIBS@

ROBJ = libyaml-dom/gen.o \
	RNACI/RNACI.o \
	populate.o \
	scan.o \
	utils.o \
	peaks.o \
	param.o \
	mod.o \
	prot.o \
	tag.o \
	search.o \
	fit.o \
	xlink.o

OBJECTS = $(ROBJ)

all: $(SHLIB)


$(SHLIB): $(OBJECTS)
$(OBJECTS): $(OBEXTRA)


clean:
	rm -rf ./libs ./*.so ./*.o
